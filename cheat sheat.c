//funcion para limpiar el buffer
while((c = getchar()) != '\n' && c != EOF);
// lo que hace es pedirle datos al buffer hasta que se tope con que esta vacio


//comando del compiler
//gcc Test.c -o test -ansi -pedantic -Wall
	
